# order-service 개발 환경 설정

# 활성화된 프로파일 설정 (dev)
spring.profiles.active=dev

# MySQL DB 연결 설정 (로컬 개발 DB)
# spring.datasource.url=jdbc:mysql://localhost:3306/bit_dev
# spring.datasource.username=bit_dev
# spring.datasource.password=bit_dev

# MyBatis 설정 (개발용)
# mybatis.mapper-locations=classpath:mapper/dev/*.xml
# mybatis.type-aliases-package=kr.co.bit.order_service.dev

# 서버 포트 설정 (개발용 포트)
server.port=8081

# 로그 레벨 설정 (개발 환경에서 더 상세한 로그 출력)
logging.level.org.springframework=DEBUG
logging.level.kr.co.bit=DEBUG

# 서비스의 다른 구성 요소 설정 (예: 개발용 Kafka 설정 등)
# 예: Kafka 설정, 메일 설정 등
# kafka.bootstrap-servers=localhost:9092
# mail.host=smtp.gmail.com

# Spring Cloud Config Server URI (외부 config 서버와 연결)
spring.cloud.config.uri=http://localhost:8888

# Cloud Config에서 설정을 가져올 때 사용할 레이블
spring.cloud.config.label=dev
